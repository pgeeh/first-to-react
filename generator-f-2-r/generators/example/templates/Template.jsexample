/**
 * Render the <%= fullName %> example
 * @param props props for the object
 * @return {object}
 */
function <%= cleanName %>(props) {
  const {test} = props;
  return (<span><%= fullName %> of {test}</span>);
}

render(
    <<%= cleanName %> test={test} />,
);
