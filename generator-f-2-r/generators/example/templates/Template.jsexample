function <%= cleanName %><%= i %>(props) {
  const {test} = props;
  return (<span><%= fullName %> of {test}</span>);
}

const initial = {
  test: 'something interesting',
};

render(
  <PropOverride initial={initial}>
    <<%= cleanName %> />
  </PropOverride>
);
