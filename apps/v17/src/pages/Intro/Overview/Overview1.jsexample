function Overview1(props) {
  const {test} = props;
  return (<span>Overview of {test}</span>);
}

const initial = {
  test: 'something interesting',
};

render(
  <PropOverride initial={initial}>
    <Overview />
  </PropOverride>
);
