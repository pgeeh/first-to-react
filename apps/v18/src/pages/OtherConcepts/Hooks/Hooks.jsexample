/**
 * Render the Hooks example
 * @param props props for the object
 * @return {object}
 */
function Hooks(props) {
  const {test} = props;
  return (<span>Hooks of {test}</span>);
}

render(
  <Hooks test={test} />
);
