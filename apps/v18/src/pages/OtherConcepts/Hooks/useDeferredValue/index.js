import info from './useDeferredValue.md';
import example1 from './DeferredFilter.jsexample';

const config = {
  info,
  name: 'useDeferredValue',
  examples: [
    {
      name: 'Deferred Filter',
      example: example1,
    },
  ],
  children: [
  ],
};

export default config;
