import info from './useId.md';
import example1 from './LabeledFields.jsexample';

const config = {
  info,
  name: 'useId',
  examples: [
    {
      name: 'Labeled Fields',
      example: example1,
    },
  ],
  children: [
  ],
};

export default config;
