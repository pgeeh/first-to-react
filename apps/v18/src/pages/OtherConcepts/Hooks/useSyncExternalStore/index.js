import info from './useSyncExternalStore.md';
import example1 from './WindowWidth.jsexample';

const config = {
  info,
  name: 'useSyncExternalStore',
  examples: [
    {
      name: 'Window Width',
      example: example1,
    },
  ],
  children: [
  ],
};

export default config;
