import info from './Refs.md';
import example1 from './FocusInput.jsexample';
import example2 from './ForwardedRef.jsexample';

const config = {
  info,
  name: 'Refs',
  examples: [
    {
      name: 'Focus Input',
      example: example1,
    },
    {
      name: 'Forwarded Ref',
      example: example2,
    },
  ],
  children: [
  ],
};

export default config;
