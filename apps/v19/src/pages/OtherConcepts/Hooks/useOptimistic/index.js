import info from './useOptimistic.md';
import example1 from './MessageList.jsexample';

const config = {
  info,
  name: 'useOptimistic',
  examples: [
    {
      name: 'Message List',
      example: example1,
    },
  ],
  children: [
  ],
};

export default config;
