import info from './use.md';
import example1 from './Promise.jsexample';
import example2 from './ConditionalContext.jsexample';

const config = {
  info,
  name: 'use',
  examples: [
    {
      name: 'Promise',
      example: example1,
    },
    {
      name: 'Conditional Context',
      example: example2,
    },
  ],
  children: [
  ],
};

export default config;
