import info from './useFormStatus.md';
import example1 from './SubmitButton.jsexample';

const config = {
  info,
  name: 'useFormStatus',
  examples: [
    {
      name: 'Submit Button',
      example: example1,
    },
  ],
  children: [
  ],
};

export default config;
