import info from './useActionState.md';
import example1 from './UpdateName.jsexample';

const config = {
  info,
  name: 'useActionState',
  examples: [
    {
      name: 'Update Name',
      example: example1,
    },
  ],
  children: [
  ],
};

export default config;
