import info from './useTransition.md';
import example1 from './FilteredList.jsexample';

const config = {
  info,
  name: 'useTransition',
  examples: [
    {
      name: 'Filtered List',
      example: example1,
    },
  ],
  children: [
  ],
};

export default config;
