import info from './Refs.md';
import example1 from './FocusInput.jsexample';
import example2 from './RefAsProp.jsexample';

const config = {
  info,
  name: 'Refs',
  examples: [
    {
      name: 'Focus Input',
      example: example1,
    },
    {
      name: 'Ref as Prop',
      example: example2,
    },
  ],
  children: [
  ],
};

export default config;
