/**
 * Render the useState example
 * @param props props for the object
 * @return {object}
 */
function useState(props) {
  const {test} = props;
  return (<span>useState of {test}</span>);
}

render(
  <useState test={test} />,
);
