
import info from './useReducer.md';
import example from './useReducer.jsexample';

const config = {
  info,
  name: 'useReducer',
  examples: [
    {
      name: 'Example 1',
      example,
    },
  ],
  children: [
  ],
};

export default config;
