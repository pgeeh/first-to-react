/**
 * Render the useReducer example
 * @param props props for the object
 * @return {object}
 */
function useReducer(props) {
  const {test} = props;
  return (<span>useReducer of {test}</span>);
}

render(
  <useReducer test={test} />,
);
