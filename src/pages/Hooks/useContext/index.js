
import info from './useContext.md';
import code from './useContext.jsexample';

const config = {
  info,
  name: 'useContext',
  code: [
    {
      name: 'Example 1',
      code,
    },
  ],
  children: [
  ],
};

export default config;
