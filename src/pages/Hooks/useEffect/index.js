
import info from './useEffect.md';
import example from './useEffect.jsexample';

const config = {
  info,
  name: 'useEffect',
  examples: [
    {
      name: 'Example 1',
      example,
    },
  ],
  children: [
  ],
};

export default config;
