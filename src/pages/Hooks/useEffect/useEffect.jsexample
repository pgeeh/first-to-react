/**
 * Render the useEffect example
 * @param props props for the object
 * @return {object}
 */
function useEffect(props) {
  const {test} = props;
  return (<span>useEffect of {test}</span>);
}

render(
  <useEffect test={test} />,
);
