
import info from './JSX.md';
import code from './JSX.jsexample';

const config = {
  info,
  name: 'JSX',
  code: [
    {
      name: 'Example 1',
      code,
    },
  ],
  children: [
  ],
};

export default config;
