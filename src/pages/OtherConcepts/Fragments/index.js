
import info from './Fragments.md';
import example from './Fragments.jsexample';

const config = {
  info,
  name: 'Fragments',
  examples: [
    {
      name: 'Example 1',
      example,
    },
  ],
  children: [
  ],
};

export default config;
