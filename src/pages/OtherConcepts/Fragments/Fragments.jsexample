/**
 * Render the Fragments example
 * @param props props for the object
 * @return {object}
 */
function Fragments(props) {
  const {test} = props;
  return (<span>Fragments of {test}</span>);
}

render(
  <Fragments test={test} />,
);
