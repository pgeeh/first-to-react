
import info from './useContext.md';
import example from './useContext.jsexample';

const config = {
  info,
  name: 'useContext',
  examples: [
    {
      name: 'Example 1',
      example,
    },
  ],
  children: [
  ],
};

export default config;
