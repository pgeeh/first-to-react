
import info from './HandlingEvents.md';
import example from './HandlingEvents.jsexample';

const config = {
  info,
  name: 'Event Handling',
  examples: [
    {
      name: 'Example 1',
      example,
    },
  ],
  children: [
  ],
};

export default config;
