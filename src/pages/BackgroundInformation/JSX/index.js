
import info from './JSX.md';
import example from './JSX.jsexample';

const config = {
  info,
  name: 'JSX',
  examples: [
    {
      name: 'Example 1',
      example,
    },
  ],
  children: [
  ],
};

export default config;
