/**
 * Render the SecondSummary
 * @param props props for the object
 * @return {object}
 */
function SecondSummary(props) {
  const {test} = props;
  return (<span>SecondSummary of {test}</span>);
}

render(
    <SecondSummary test={test} />,
);
