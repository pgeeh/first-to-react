
import info from './useReducer.md';
import code from './useReducer.jsexample';

const config = {
  info,
  name: 'useReducer',
  code: [
    {
      name: 'Example 1',
      code,
    },
  ],
  children: [
  ],
};

export default config;
