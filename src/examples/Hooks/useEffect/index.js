
import info from './useEffect.md';
import code from './useEffect.jsexample';

const config = {
  info,
  name: 'useEffect',
  code: [
    {
      name: 'Example 1',
      code,
    },
  ],
  children: [
  ],
};

export default config;
