/**
 * Render the Redux and Flux Workflows example
 * @param props props for the object
 * @return {object}
 */
function ReduxandFluxWorkflows(props) {
  const {test} = props;
  return (<span>Redux and Flux Workflows of {test}</span>);
}

render(
  <ReduxandFluxWorkflows test={test} />,
);
