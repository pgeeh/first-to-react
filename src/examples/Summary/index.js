
import info from './Summary.md';
import code from './Summary.jsexample';
import SecondSummary from '../SecondSummary';

const config = {
  info,
  name: 'Summary',
  code,
  children: [
    SecondSummary,
  ],
};

export default config;
