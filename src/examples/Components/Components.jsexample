/**
 * Render the Components example
 * @param props props for the object
 * @return {object}
 */
function Components(props) {
  const {test} = props;
  return (<span>Components of {test}</span>);
}

render(
    <Components test={test} />,
);
