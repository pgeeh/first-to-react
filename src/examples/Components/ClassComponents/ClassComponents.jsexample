/**
 * Render the ClassComponents example
 * @param props props for the object
 * @return {object}
 */
function ClassComponents(props) {
  const {test} = props;
  return (<span>ClassComponents of {test}</span>);
}

render(
  <ClassComponents test={test} />,
);
