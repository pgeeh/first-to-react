/**
 * Render the Handling Events example
 * @param props props for the object
 * @return {object}
 */
function HandlingEvents(props) {
  const {test} = props;
  return (<span>Handling Events of {test}</span>);
}

render(
  <HandlingEvents test={test} />,
);
