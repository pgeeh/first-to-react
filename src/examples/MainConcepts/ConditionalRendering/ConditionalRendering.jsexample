/**
 * Render the Conditional Rendering example
 * @param props props for the object
 * @return {object}
 */
function ConditionalRendering(props) {
  const {test} = props;
  return (<span>Conditional Rendering of {test}</span>);
}

render(
  <ConditionalRendering test={test} />,
);
