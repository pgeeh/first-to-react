/**
 * Render the Forms Uncontrolled example
 * @param props props for the object
 * @return {object}
 */
function FormsUncontrolled(props) {
  const {test} = props;
  return (<span>Forms Uncontrolled of {test}</span>);
}

render(
  <FormsUncontrolled test={test} />,
);
