
import info from './FormsUncontrolled.md';
import code from './FormsUncontrolled.jsexample';

const config = {
  info,
  name: 'Forms Uncontrolled',
  code,
  children: [
  ],
};

export default config;
