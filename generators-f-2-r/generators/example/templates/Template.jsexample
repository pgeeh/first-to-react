/**
 * Render the <%= cleanName %>
 * @param props props for the object
 * @return {object}
 */
function <%= cleanName %>(props) {
  const {test} = props;
  return (<span><%= cleanName %> of {test}</span>);
}

render(
    <<%= cleanName %> test={test} />,
);
