{
  "name": "first-to-react",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "overrides": {
    "react-markdown": {
      "react": "^17.0.1"
    }
  },
  "scripts": {
    "start:v17": "npm run start --workspace=apps/v17",
    "build:v17": "npm run build --workspace=apps/v17",
    "test:v17": "npm run test --workspace=apps/v17"
  }
}
